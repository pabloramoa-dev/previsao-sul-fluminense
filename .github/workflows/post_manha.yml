name: Post Manha (06h BRT)

on:
  schedule:
    - cron: "0 9 * * *"   # 06:00 BRT (UTC-3)
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Rodar em modo dry-run (nao publica)"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  publicar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Gerar e publicar carrossel da manha
        env:
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
        run: |
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            python -m src.main manha --dry-run
          else
            python -m src.main manha
          fi

      - name: Commit imagens e estado
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/output src/estado.json src/historico.json || true
          git commit -m "chore: post manha $(date -u +%Y-%m-%d)" || echo "Nada para commitar"
          git push || echo "Nada para enviar"
