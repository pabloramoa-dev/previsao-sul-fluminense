name: Monitor de Alertas (a cada 2h, 7h-19h BRT)

on:
  schedule:
    - cron: "0 10-22/2 * * *"   # 07h-19h BRT, de 2 em 2 horas
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Rodar em modo dry-run (nao publica)"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  monitorar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Cache do Chromium do Playwright
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-v1

      - name: Dependencias de sistema do Chromium (tolerante a falha)
        id: deps
        run: |
          python -m playwright install-deps chromium \
            || echo "::warning::playwright install-deps falhou; seguindo com as libs da imagem do runner"

      - name: Baixar o Chromium (renderiza os cards em HTML)
        id: chromium
        run: python -m playwright install chromium

      # Ate 21/08/2026 este passo gerava o card E mandava o Instagram busca-lo
      # em raw.githubusercontent.com na mesma execucao - mas o commit das
      # imagens so acontecia no fim do job. O Instagram batia numa URL que
      # ainda nao existia e devolvia "The media could not be fetched from this
      # URI", derrubando o job toda vez que um alerta disparava. Agora o
      # monitor segue o mesmo desenho dos outros quatro workflows do repo:
      # gera -> commita -> espera propagar -> publica.
      - name: Detectar alertas e gerar os cards (sem publicar)
        id: detectar
        env:
          PUBLICAR_FASE: gerar
        run: |
          set -o pipefail
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            python -m src.main alertas --dry-run 2>&1 | tee /tmp/alertas.log
          else
            python -m src.main alertas 2>&1 | tee /tmp/alertas.log
          fi

      # So as imagens. O src/estado.json (anti-spam) fica de fora de proposito:
      # se a publicacao falhar depois, o alerta NAO pode ficar marcado como ja
      # enviado - ele precisa poder tentar de novo no ciclo seguinte.
      - name: Commit e push dos cards (antes de publicar)
        if: ${{ inputs.dry_run != true }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/output || true
          git commit -m "chore: cards de alerta $(date -u +%Y-%m-%dT%H:%MZ)" || echo "Nada para commitar"
          git push || echo "Nada para enviar"

      - name: Aguardar propagacao do raw.githubusercontent
        if: ${{ inputs.dry_run != true }}
        run: sleep 45

      - name: Publicar no Instagram
        id: publicar
        if: ${{ inputs.dry_run != true }}
        env:
          PUBLICAR_FASE: publicar
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
        run: |
          set -o pipefail
          python -c "from src import publicar; publicar.publicar_fila()" 2>&1 | tee -a /tmp/alertas.log

      - name: Coletar motivo da falha
        if: failure()
        id: motivo
        run: |
          if [ "${{ steps.chromium.outcome }}" = "failure" ]; then
            echo "passo=Baixar o Chromium" >> "$GITHUB_OUTPUT"
          elif [ "${{ steps.detectar.outcome }}" = "failure" ]; then
            echo "passo=Detectar alertas e gerar os cards" >> "$GITHUB_OUTPUT"
          elif [ "${{ steps.publicar.outcome }}" = "failure" ]; then
            echo "passo=Publicar no Instagram" >> "$GITHUB_OUTPUT"
          else
            echo "passo=(anterior ao detector - ver log)" >> "$GITHUB_OUTPUT"
          fi
          {
            echo "trecho<<FIM"
            tail -n 25 /tmp/alertas.log 2>/dev/null || echo "(sem saida capturada)"
            echo "FIM"
          } >> "$GITHUB_OUTPUT"

      - name: Commit estado (anti-spam)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/output src/estado.json || true
          git commit -m "chore: monitor alertas $(date -u +%Y-%m-%dT%H:%MZ)" || echo "Nada para commitar"
          git push || echo "Nada para enviar"

      - name: Avisar falha por e-mail
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "[Previsao Sul Fluminense] FALHOU: ${{ github.workflow }}"
          to: pablo.ramoa@gmail.com
          from: Bot Previsao Sul Fluminense <${{ secrets.MAIL_USERNAME }}>
          body: |
            O workflow "${{ github.workflow }}" falhou e NAO publicou.

            PASSO QUE FALHOU: ${{ steps.motivo.outputs.passo }}

            Ultimas linhas da saida:
            ---------------------------------------------
            ${{ steps.motivo.outputs.trecho }}
            ---------------------------------------------

            Repositorio: ${{ github.repository }}
            Disparo....: ${{ github.event_name }}
            Log completo: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
